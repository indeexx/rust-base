// #[macro_use]
extern crate log;

fn main() {
    // env_logger::init();
    let str = ["indeex", "hk"];
    // info!("{:?}", str);
    // warn!("{:?}", str);
    // error!("{:?}", str);
    // trace!("{:?}", str);
    // debug!("{:?}", str);
    // info!("starting up");
    println!("{:?}", str);
}
